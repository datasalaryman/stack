on:
  push:
    branches:
       - master

jobs:
  test_rstudio_container_run:
    name: Test VPN container run
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@master
      - name: Run docker container in actions run
        run: > 
          cd vpn &&
          make run
